# MPPI velocity-range overlay for allow_reverse:=true: allow reverse sampling.
# Reverse capped slower than forward (vx_max 2.5) — cautious by default.
#
# REEDS_SHEPP paths contain reverse segments. Two stock-critic settings made MPPI
# drive them the WRONG WAY (steer away from the goal, circle forever):
#   * PreferForwardCritic penalized the reverse motion the path demanded;
#   * PathAlignCritic ignored path orientations, tracking reverse segments
#     positionally — i.e. driving them forwards.
controller_server:
  ros__parameters:
    FollowPath:
      vx_min: -1.5
      # Weak forward bias, NOT disabled: with it fully off, a single optimizer
      # hiccup left MPPI in a "keep backing away" local optimum for 40 s (nothing
      # penalized persistent reverse once GoalCritic was out of range). Weight 1.0
      # escapes that optimum but still loses to PathAlign (14.0) on genuine
      # reverse segments — verified: 8 m behind-goal still reversed and reached.
      PreferForwardCritic:
        enabled: true
        cost_weight: 1.0
      PathAlignCritic:
        use_path_orientations: true
